-- SELECT * FROM products_master_fulltext WHERE MATCH (`description`,`name`,`brand`,`type`) AGAINST ('pool' IN BOOLEAN MODE)

SELECT *, MATCH (`description`,`name`,`brand`,`type`) AGAINST ('polaris') as score FROM products_master_fulltext WHERE MATCH (`description`,`name`,`brand`,`type`) AGAINST ('polaris') > 0 ORDER BY score DESC;
